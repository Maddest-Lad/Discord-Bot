Linked List
Group ----->
Members[]

Each Group Points to Another and If Everyone Leaves A Group Then it Will Be Deleted
//Likely Methods
boolean hasMembers(group[]);

if(!hasMembers(group[i]) {
    delete(group[i]);
}

Iterates Through the Linkedlist Of Groups When it Finds the Group then It Returns the Id's of the Members and Discord
Users can Add/Delete Nodes After A Permission Check ie: Only The Person Can Leave a Group, Compares Known Id to Current Id.

Public Class LinkedList

Class Node {
Node next;
String[] Users;
}