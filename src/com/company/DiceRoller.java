package com.company;

public class DiceRoller {
/*
    public String Roll() {
        String command = cleanup(message);

    }

    public static String cleanup(@NotNull String message) {
        //Remove WhiteSpace & Cast To Lowercase
        String parsedString = message.toLowerCase();
        parsedString = parsedString.replaceAll("\\s+","");

        return parsedString;
    }

    public static int Roll(int max) {
        return (int) (Math.random() * ((max - 1) + 1)) + 1;
    }
*/

}
